You are an autonomous coding agent.
Use the $prd skill to create a Product Requirements Document in JSON.
Save the PRD as JSON in directory: /Volumes/Data/gmgame/gmgame-site-backend/.agents/tasks
Filename rules: prd-<short-slug>.json using 1-3 meaningful words.
Examples: prd-workout-tracker.json, prd-usage-billing.json
Do NOT implement anything.
After creating the PRD, end with:
PRD JSON saved to <path>. Close this chat and run `ralph build`.

User request:
В бэкенде сайта после POST /vote_handler вызывается Bot API send_embed из applyVoteReward: external-api.service.ts (/Volumes/Data/gmgame/gmgame-site-backend/src/external-api/external-api.service.ts:348), external-api.controller.ts (/
  Volumes/Data/gmgame/gmgame-site-backend/src/external-api/external-api.controller.ts:98), data-provider.service.ts (/Volumes/Data/gmgame/gmgame-site-backend/src/data-provider/data-provider.service.ts:59).
  В ../GMGameBot сообщение о голосе отправляется в POST /send_embed: routes.js (/Volumes/Data/gmgame/GMGameBot/routes.js:98).
  На сервере root@46.0.203.33:2134 старый формат уведомления находится в Discord_helper/routes/vote_handler.js (rich embed + кнопки + текст с мониторингом).

  Нужно перенести формат Discord-уведомления о голосе из старого бота в новый ../GMGameBot, не ломая текущую интеграцию сайта.

  Контекст:
  - Сайт вызывает /api/vote_handler, после чего в applyVoteReward отправляет запрос в Bot API: send_embed (payload сейчас { username, prize: "money" }).
  - Новый бот: ../GMGameBot/routes.js, роут POST /send_embed.
  - Старый бот с нужным форматом уведомления: /root/____NEW_DATA_FOLDER/Discord_helper/routes/vote_handler.js (там красивый embed и кнопки).
  - Переносим именно формат сообщения в новый бот.

  Что сделать:
  1 В ../GMGameBot/routes.js в POST /send_embed заменить текущий контент embed/кнопок на стиль из старого Discord_helper/routes/vote_handler.js:
  - з
  - описание «Спасибо за голос...»
  - кнопки: помощь проекту, инфо о голосованиях, ссылки на мониторинги.
  2) Сохранить совместимость payload:
  - поддержать как минимум поля username/user и monitoring.
  - если monitoring не передан, показывать нейтральный вариант (без «на <monitoring>» или с fallback-текстом).
  3) Не переносить из старого бота RCON-выдачу приза и логику случайного выигрыша (она в другом месте); переносим только Discord-сообщение.
  4) В бэкенде сайта (gmgame-site-backend) доработать отправку в send_embed, чтобы передавался monitoring (если доступен в контексте voteHandler).
  - при этом не сломать текущую выдачу награды/баланса.
  - при эвить минимальную защиту от пустого имени пользователя в send_embed (fallback Unknown).
  6) Обновить/добавить e2e/unit тесты для сценариев:
  - payload только с username
  - payload с user + monitoring
  - отсутствие monitoring
  7) Ничего не менять в прод-конфигах и секретах. Только код + тесты.

  Критерии приёмки:
  - После голоса в Discord уходит новое сообщение в стиле старого бота, но через ../GMGameBot.
  - API /send_embed не падает на старом payload.
  - Сайт продолжает начислять награду как раньше.
  - Все тесты проходят.
  PROMPT
  )
